# Example outputs

This directory contains representative output figures generated by scripts in
the `scripts/` directory.

These files are provided **for reference and visual verification only** and are
**not required inputs** for running the analysis.

They illustrate the expected format and appearance of plots produced by the
pipeline.

